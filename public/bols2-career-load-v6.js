/* BOLS2 Career Load — local device archive only. */
(function(){
 'use strict';
 const AUTO='BOLS2_AUTOSAVE_V2',LEGACY='bol2_saves_v1',SLOTS=5;
 const tick=s=>((+s?.year||1)-1)*52+(+s?.week||1);
 const clone=s=>{try{return JSON.parse(JSON.stringify(s))}catch{return null}};
 const studioName=s=>String((s?.studio&&s.studio.name)||s?.studioName||s?.name||'Unnamed Studio');
 const read=k=>{try{return JSON.parse(localStorage.getItem(k)||'null')}catch{return null}};
 function all(){const out=[];const a=read(AUTO);if(a?.state)out.push({state:a.state,source:'AUTOSAVE',slot:+a.state.saveSlot||+a.state.slot||1,savedAt:a.savedAt||''});const l=read(LEGACY);if(Array.isArray(l))l.forEach((s,i)=>s&&out.push({state:s,source:'LOCAL SLOT '+(i+1),slot:i+1,savedAt:s.updatedAt||s.savedAt||''}));return out.filter(x=>x.state&&typeof x.state==='object')}
 function pick(){const map=new Map();all().forEach(c=>{const k=c.slot||1;if(!map.has(k)||tick(c.state)>tick(map.get(k).state)||(tick(c.state)===tick(map.get(k).state)&&String(c.savedAt)>String(map.get(k).savedAt)))map.set(k,c)});return [...map.values()].sort((a,b)=>tick(b.state)-tick(a.state)||String(b.savedAt).localeCompare(String(a.savedAt)))}
 function apply(c){const s=clone(c?.state||c);if(!s)return false;s.saveSlot=Math.max(1,Math.min(SLOTS,Number(c?.slot||s.saveSlot||s.slot)||1));const live=window.__BOL_STATE__||window.state;if(live&&typeof live==='object'){Object.keys(live).forEach(k=>{try{delete live[k]}catch{}});Object.assign(live,s);window.state=live;window.__BOL_STATE__=live;window.__BOL_CURRENT_STATE__=live;window.start?.(live)}else{window.state=s;window.__BOL_STATE__=s;window.__BOL_CURRENT_STATE__=s;window.start?.(s)}try{localStorage.setItem(AUTO,JSON.stringify({version:7,reason:'explicit-load',savedAt:new Date().toISOString(),state:clone(s)}))}catch{}return true}
 function render(){document.querySelector('#bols2-career-load-v6')?.remove();const wrap=document.createElement('div');wrap.id='bols2-career-load-v6';wrap.innerHTML='<div class="bcl6-backdrop"><section class="bcl6-sheet"><div class="bcl6-head"><div><div class="bcl6-kicker">CAREER ARCHIVE</div><h2>Load Your Saves</h2><p>Each studio lives in its own save slot.</p></div><button data-bcl-close>×</button></div><div class="bcl6-list"></div></section></div>';document.body.appendChild(wrap);const list=wrap.querySelector('.bcl6-list'),items=pick(),bySlot=new Map(items.map(c=>[Number(c.slot)||1,c]));for(let slot=1;slot<=SLOTS;slot++){const c=bySlot.get(slot),row=document.createElement('div');row.className='bcl6-row'+(c?'':' empty');if(c){const s=c.state;row.innerHTML=`<div><div class="bcl6-slot">SLOT ${slot}</div><strong>${studioName(s)}</strong><div class="bcl6-meta">Year ${+s.year||1} · Week ${+s.week||1}</div><div class="bcl6-money">$${Number(s.money||0).toLocaleString()}</div><small>${c.source}</small></div><button data-bcl-load="${slot}">LOAD</button>`}else{row.innerHTML=`<div><div class="bcl6-slot">SLOT ${slot}</div><strong>EMPTY</strong><div class="bcl6-meta">Available for a new studio career.</div></div><span class="bcl6-emptyAction">NEW CAREER →</span>`}list.appendChild(row)}wrap.addEventListener('click',e=>{if(e.target.matches('[data-bcl-close]')||e.target.classList.contains('bcl6-backdrop')){wrap.remove();return}const b=e.target.closest('[data-bcl-load]');if(!b)return;const c=bySlot.get(Number(b.dataset.bclLoad));if(c&&apply(c))wrap.remove()})}
 window.__BOLS2_OPEN_LOAD_V6__=render;window.BOLS2CareerLoadV6={render,pick,apply,all,tick};
})();