/* BOLS2 weekly-flow repair: legacy event bridge disabled. The v4 weekly flow owns the transition so the game cannot wrap the Continue button multiple times. */
(()=>{ window.__v3BridgeDisabled = true; })();